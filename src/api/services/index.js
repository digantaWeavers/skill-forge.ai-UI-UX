/**
 * index.js (Services Entrypoint)
 * 
 * Export all services from here for easier, cleaner imports elsewhere.
 */

export { authService } from './authService';
export { userService } from './userService';
